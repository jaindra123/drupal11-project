// Simple JS for Events Theme
(function ($, Drupal) {
  Drupal.behaviors.eventsTheme = {
    attach: function (context, settings) {
      console.log('Events Theme JS Loaded');
    }
  };
})(jQuery, Drupal);
